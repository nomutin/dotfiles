name: shellcheck

on:
  push:
    branches:
      - main
    pull_request:

jobs:
  test:
    name: shellcheck
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: shellcheck
        run: make shellcheck
